export const TooltipConstants = Object.freeze({
    HOME_SIDE_OFFSET: 20,
    DELAY_DURATION: 200,
})

export enum TooltipSide {
    TOP = 'top',
    RIGHT = 'right',
    BOTTOM = 'bottom',
    LEFT = 'left',
}